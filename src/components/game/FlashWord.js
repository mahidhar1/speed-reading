import React from "react";

function FlashWord() {
  return (
    <div>
      Words are flashed and then user is given multiple options User has to
      choose from those option the right word that was flashed
    </div>
  );
}

export default FlashWord;
